# CLAUDE.md

Read `AGENTS.md` first.

Career Alpha contains eight independent but composable skills under `skills/`. Route to the narrowest relevant skill and preserve the shared evidence-first rules. Do not invent facts to make a candidate look stronger; use `/build` or `/contributor` to create missing proof, `/proof` to audit it, and `/position` to express it.
